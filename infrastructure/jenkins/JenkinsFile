pipeline{
    agent any
    environment{
        rollback = 'false'
    }
    stages{
        stage("Dependencies"){
            steps{
                sh "bash jenkins/scripts/install-dependencies.sh"
            }
        }
    }
}