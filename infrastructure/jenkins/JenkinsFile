pipeline{
    agent any
    environment{
    }
    stages{
        stage("Dependencies"){
            steps{
                sh "bash jenkins/scripts/install-dependencies.sh"
            }
        }
    }
}